.DS_Store
__pycache__/
.ipynb_checkpoints/
/plan

# User-generated experiment outputs (figures, CSVs, JSONs, checkpoints).
# Re-created by `python main.py` (and Colab runs that call `main.main`).
results/*
!results/.gitkeep
/.vscode
.DS_Store

# Author-side material kept out of the public repo (files stay on local disk;
# untracked via `git rm -r --cached`, see commit history): design docs,
# regression tests, agent config, CI workflows.
/docs/
/tests/
/.claude/
/.github/workflows/